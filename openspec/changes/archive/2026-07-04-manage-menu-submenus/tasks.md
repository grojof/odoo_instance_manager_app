# Tasks

## 1. Spec deltas

- [x] 1.1 instance-configuration: ADDED "Grouped management menu".
- [x] 1.2 `openspec validate manage-menu-submenus --strict` passes.

## 2. Code

- [x] 2.1 `manage.py`: two-level menu (Status & health / Configuration / Backups & duplication / Delete).

## 3. Docs & changelog

- [x] 3.1 `docs/operations/instance-management.md`: mention the grouped submenus.
- [x] 3.2 `CHANGELOG.md` `[Unreleased]`.

## 4. Verify

- [x] 4.1 `openspec validate --strict`, `ruff`, `pytest`, `compileall` pass.
